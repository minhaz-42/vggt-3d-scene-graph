\begin{figure}[tb]
\centering
\includegraphics[width=0.70\linewidth]{fig_dedup3d.png}
\caption{Duplicate-aware fusion on the real \texttt{freiburg1\_desk} cloud (ten views; desaturated so
the object nodes stand out). \textbf{Left:} \textsc{graph-fusion} leaves the single monitor as ten nodes
and the desk as nine, the over-counting that dominates the precision loss. \textbf{Right:} the merge
collapses same-label overlapping nodes ($34\!\to\!13$ nodes) into a clean labeled graph. (Shown by 3D
proximity here; the metric uses 3D-box IoU, Section~\ref{sec:exp_variants}.)}
\label{fig:dedup3d}
\end{figure}
