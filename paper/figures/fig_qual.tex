\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_qual.pdf}
\caption{Duplicate-aware fusion on a real desk scene (\texttt{freiburg1\_desk}, ten views),
fused-node centroids projected onto their top two principal axes. \textbf{Left:} cross-view
\textsc{graph-fusion} leaves the single monitor as ten nodes and the desk as nine (dashed rings) ---
the over-counting that dominates the precision loss. \textbf{Right:} the duplicate-aware merge
collapses same-label overlapping nodes into one object each ($34\!\to\!13$ nodes), recovering a clean
labeled object--relation graph. (Merging is shown by 3D proximity for this illustration; the benchmark
metric uses 3D-box IoU, Section~\ref{sec:exp_variants}.)}
\label{fig:qual}
\end{figure}
